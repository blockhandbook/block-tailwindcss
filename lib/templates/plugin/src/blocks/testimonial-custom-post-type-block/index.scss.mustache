/**
 * The following styles get applied inside the editor only.
 *
 * Replace them with your own styles or remove the file completely.
 */
// Escape forward slashes, otherwise they won't compile
// i.e. .w-1\/3 {}

.wp-block-{{slug}}-testimonial-custom-post-type-block {
	// write editor only SASS here
}
{{#noCssFramework}}

// Some custom styling for media upload controls
[class*="{{slug}}"] {
	.media-upload-btn {
		height: 6rem;

		img {
			height: 6rem;
			margin:auto
		}
	}

	.ml-auto {
		margin-left: auto;
	}
}
{{/noCssFramework}}
